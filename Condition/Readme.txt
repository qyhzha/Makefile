学习小结：
	makefile中支持条件判断语句。
		可以根据条件的值决定make的执行
		可以比较两个不同变量或者比较变量和常量。
	注意：条件判断语句只能用于控制实际执行的语句，不能控制规则中命令的执行过程。
	
	具体的条件关键字：
		ifeq、ifneq、ifdef、ifndef
	
	条件判断语句之前可以有空格，但不能有tab键。
	在条件语句中不要使用自动变量。
	一条完整的条件判断语句必须位于同一个makefile中。